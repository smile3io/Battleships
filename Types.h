#pragma once

enum class Render {
    SHIPS, SHOTS, ALL       // render 
};





enum class UI {
    PLACE, SHOOT, MULTI    // decides which UI 
};

enum class Input {
    ENTER = 13,
    R = 114,
    UP = 72, 
    DOWN = 80,
    LEFT = 75,
    RIGHT = 77
};

